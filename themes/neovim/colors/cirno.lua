-- cirno colorscheme entrypoint.
-- Lets `:colorscheme cirno` work. Respects the current &background via
-- the default "auto" variant, unless you've called require("cirno").setup{}.
require("cirno").load()
